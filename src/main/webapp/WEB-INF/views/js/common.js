$(document).ready(function ( event ) {
	$(".login-wrap").css({
		marginTop : ( $(window).height() - $(".login-wrap").height() ) / 2,
		marginBottom : ( $(window).height() - $(".login-wrap").height() ) / 2
	});
});